# Include this file in your module's `package.yaml` to set sensible lorentz
# defaults and get access to useful utilities.

<<: !include "definitions.yaml"

<<: *meta
default-extensions: *default-extensions
ghc-options: *ghc-options
